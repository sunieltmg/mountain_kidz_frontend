import 'package:flutter/material.dart';

class TabAttendance extends StatelessWidget {
  const TabAttendance({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(children: [
        
      ]),
    );
  }
}
