import 'package:flutter/material.dart';

class TabResult extends StatelessWidget {
  const TabResult({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}