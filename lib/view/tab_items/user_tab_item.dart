import 'package:flutter/material.dart';

class UserTabItem extends StatelessWidget {
  const UserTabItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
